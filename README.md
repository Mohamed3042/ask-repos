# ask-repos

> Ask any GitHub account about its repositories — every answer cites file and line, or refuses.

Work in progress. The full README (Verify in two minutes, architecture, retrieval table,
MCP setup, the injection stance) lands with `v0.1.0`.
